# Production backend URL — update this to the real deployed backend before building.
# This file is committed to source control; do NOT put secrets here.
VITE_API_BASE_URL=https://api.wymca.org/api/v1

